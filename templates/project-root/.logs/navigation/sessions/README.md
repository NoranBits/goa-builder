# navigation sessions

## Purpose

Store append-only navigation session entries here.

## Naming

Use:

- `YYYY/MM/DD/<timestamp>_<actor>_<topic>.md`

Where `timestamp` is UTC and `topic` is short (3 to 6 words).
