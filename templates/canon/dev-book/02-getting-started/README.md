# 02 Getting Started

## Quick Start (Wizard)

This repository should use a deterministic setup wizard or script.

## Common Prerequisites (Example)
- Container Runtime (Docker/Podman)
- Language Runtime (Node/Python/Rust)
- Package Manager (pnpm/bun/cargo/pip)

## Installation Steps (Placeholder)
1. Clone the repository.
2. Run the setup script (e.g., `./scripts/setup.sh`).
3. Build the project.

## Troubleshooting
- Check `.logs/service` for background task failures.
- Verify environment variables in `.env`.

