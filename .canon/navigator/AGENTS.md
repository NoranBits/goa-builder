# AGENTS.md (Navigator Gate)

This gate applies to files under `.canon/navigator/**`.

## Rules

- Navigator pages are generated; avoid hand-editing generated pages.
- Put human notes and decisions in `.logs/**`.
- If you need to adjust how pages are generated, change `.builder/builder.py` instead.

## Placeholders (adjust per repo)

- Navigator regeneration command: `{{NAV_REGEN_COMMAND}}`
